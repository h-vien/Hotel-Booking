import React from "react";
import styles from "./style.module.scss";
const SearchItemResult = () => {
  return (
    <div className={styles.sortBar}>
      <span className={styles.sortBarLabel}></span>
    </div>
  );
};

export default SearchItemResult;
